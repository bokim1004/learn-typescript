// function sum(a, b) {
//     return a + b;
// }

// sum(10, "20"); //1020
//javascript doc과 ts-check통해서 자바스크립트에서 타입스크립트 같은 효과를 줄 수 있다
//@ts-check

/**
 * @param {number} a 첫번째 숫자
 * @param {number} b 두번째 숫자
 */

function sum(a, b) {
    return a + b;
}
sum(10, "20");
